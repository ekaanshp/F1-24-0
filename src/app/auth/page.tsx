// =============================================================================
// Auth Page — /auth
// =============================================================================
// Login / Sign up page. NextAuth handles the OAuth flows.
// TODO: Build the authentication UI (Google + Discord sign-in buttons)
// =============================================================================

export default function AuthPage() {
  return (
    <main>
      <h1>Sign In — F1 TeamBuilder</h1>
      <p>Authentication page placeholder. Implement OAuth sign-in UI here.</p>
    </main>
  );
}
