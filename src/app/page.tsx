// =============================================================================
// Landing Page — / (Home)
// =============================================================================
// The hook: explains the 24-0 challenge, shows recent winners,
// and provides a clear "Start Draft" call to action.
//
// TODO: Build the landing page UI with hero section, game explanation, CTA
// =============================================================================

export default function HomePage() {
  return (
    <main>
      <h1>F1 TeamBuilder — The 24-0 Challenge</h1>
      <p>Landing page placeholder. Build the hero section and game introduction here.</p>
    </main>
  );
}
